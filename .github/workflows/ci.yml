---
name: CI

on:
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: MegaLinter
        run: ./scripts/lint -c
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
